This directory is for doing mockups on the wire-frame.

It should contain client-site code, such as HTML/CSS/etc.  This wire-frame will then be used to create a template for whichever tool we decide to use to run the website.
